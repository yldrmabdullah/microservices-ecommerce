spring.application.name=ui
server.port=8085

# Thymeleaf
spring.thymeleaf.cache=false

# Logging
logging.level.com.valven.ecommerce=INFO
logging.level.org.springframework.web=WARN

# External service URLs (for production)
app.services.product-service.url=http://gateway:8080/api/products
app.services.order-service.url=http://gateway:8080/api